<Window x:Class="AE.UserInterface.Views.Deviations.Impl.DeviationCollectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DeviationCollectionView" MinHeight="300" MinWidth="500" SizeToContent="WidthAndHeight" MaxWidth="600">
    <Grid>
        <DataGrid Name="Deviations" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="Auto"/>
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>