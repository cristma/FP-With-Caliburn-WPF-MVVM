<UserControl x:Class="AE.UserInterface.Views.Approaches.Impl.ApproachEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" MinWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="0">Name:</Label>
        <Label Grid.Row="1" Grid.Column="0">Criteria:</Label>
        <TextBox Name="ApproachName" AcceptsReturn="False" AcceptsTab="False" MaxLength="200" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center"/>
        <ComboBox Name="Criterias" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center"/>
        <Button Name="Ok" Grid.Row="2" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" MinWidth="75">OK</Button>
    </Grid>
</UserControl>